# kabinet-dashboard

Streamlit-дашборд поверх Databricks Lakebase (Postgres). Подключение — только через
`db/connection.py`, креды — только через `st.secrets`, никогда в коде.

## Практики Databricks

### Каждая ячейка ноутбука самодостаточна

Ячейка заводит своё подключение и объявляет свои константы. Не полагаемся на то, что
выше по ноутбуку что-то уже выполнилось и оставило переменную в области видимости.

Причина в том, как ноутбуки реально исполняются: ячейку перезапускают в одиночку,
запускают с середины после падения, копируют в job — и любое состояние из соседних ячеек
в этот момент отсутствует или протухло. Ячейка, которая работает только при прогоне
сверху вниз, ломается ровно тогда, когда её отлаживают.

Дублирование подключений и констант здесь — не копипаста, а цена за то, что каждую
ячейку можно запустить в отрыве от остальных.

### `restartPython()` — один в ячейке, и ничего после него

`dbutils.library.restartPython()` перезапускает интерпретатор и обрывает
выполнение всего, что стоит после него в той же ячейке. «Run All» на этом
месте не падает, а молча останавливается: ни ошибки, ни красного — просто
остальной код не выполнился. Так ломались AMC Collect, FBA Ledger Detail
Loader и Stock Loader.

Поэтому установка пакетов — отдельная первая ячейка: `%pip install …` и
`restartPython()`, больше ничего. Весь код — со второй ячейки, и она
самодостаточна: после рестарта из первой не остаётся ни одной переменной.

Не полагаться на то, что `%pip` в новых рантаймах перезапускает Python сам:
где-то перезапускает, где-то нет, а ноутбук должен работать одинаково.

### Метка в `system_pulse` ставится в конце безусловно

Запись в `kabinet_data.system_pulse` — последняя операция ячейки, на верхнем уровне.
Не внутри `if всё_прошло:`, не внутри `try` рядом с полезной работой.

`system_pulse` — внешняя точка контроля: если умрут и сторож, и загрузчики, инциденты
никто не создаст, но метка перестанет обновляться, и это будет видно.

С 26.08.2026 в интерфейсе этого сигнала нет вообще: плашку свежести с Обзора убрали,
клиенту состояние загрузчиков не показываем. Решение окончательное, индикатор в
дашборд не возвращаем — не предлагать его как решение задач про свежесть данных.

Единственный канал алертов по живости загрузчиков — Telegram. «Инциденты» сюда не
относятся: там данные о качестве самих данных, а это другой сигнал (см. абзац ниже).
Поэтому вопрос «почему цифры старые» с дашборда больше не отвечается — ответ в
Telegram и в `system_pulse` напрямую.

Метка, спрятанная в условную ветку, ломает этот контур в обе стороны: загрузчик отработал,
но по какой-то строгой проверке в ветку не зашёл — приходит алерт «загрузчик мёртв»,
хотя данные на месте. Ложные тревоги приучают не читать эти сообщения, и настоящий сбой
проходит незамеченным. Теперь, когда канал один и он в мессенджере, цена ложной
тревоги выше: на дашборде плашку можно было проигнорировать взглядом, а поток
неверных алертов в Telegram отключают целиком.

Из-за этого `last_success_at` по смыслу — «когда ячейка доработала до конца», а не
«когда всё внутри отработало без ошибок». За качество самих данных отвечают инциденты,
за живость загрузчика — пульс. Это разные сигналы, смешивать их в одном поле не надо.

### Проглоченная ошибка обязана дойти туда, где её прочтут

`try/except` вокруг проверки или отправки — нормально: одна упавшая проверка
не должна оставлять без присмотра остальные. Ненормально то, что делается в
`except`. `print("⚠️ …")` в лог джобы — это не логирование: лог никто не
открывает, джоба зелёная, пульс на месте. Именно так `Kabinet - Watchdog`
с 01.09 по 13.09.2026 на каждом прогоне печатал `Telegram error` и две
недели не отправил ни одного алерта, а `Suppressed Listings Checker` десять
дней падал на SSL-обрыве, и никто не заметил, потому что правила на него
не было.

Правило: ошибку, которую решили проглотить, кладём туда, где её увидят, —
в само сообщение Telegram отдельным блоком («⚙️ сбои проверок сторожа»),
в инцидент, в `_soft_errors`. А если проглатывать нельзя — как отправку
самого алерта — прогон должен упасть, но после записи пульса, чтобы падение
не превратилось в ложный «сторож мёртв». Молчание канала должно быть красным.

### Одно имя джоба в трёх местах

Имя джоба в Databricks, имя в метке `system_pulse` и `job_name` в правиле
`job_health_rules` — одна и та же строка, символ в символ. Расхождение не ломает
ничего явно, поэтому живёт месяцами и обнаруживается только сплошной сверкой.

Ломается это в обе стороны, и обе тихие.

Правило без метки: сторож ждёт имя, которое никто не пишет, и каждый день шлёт
«no success in N h» про живую джобу. Так было с `Kabinet - Review Requests
(Morning)` и `(ES Evening)` — два правила на два расписания, а ноутбук писал одну
метку `Kabinet - Review Requests`.

Метка без правила: джоба пишет пульс, но её никто не проверяет — умрёт молча.
Так остались без присмотра `Kabinet - MYI Report Collector`, `FBA Ledger Detail
Loader`, `Listing Cards Sync`, `CF Transactions Loader`, `MM Competitiveness Loader`.

Переименование даёт сразу обе поломки: старая метка застывает и выглядит мёртвой
джобой, новая пишется мимо правила. Именно так `Kabinet - AMC Collect` (метка от
31.08) породил ежедневный ложный алерт, пока живой `AMC Collect` писал рядом
каждые шесть часов.

Практическое следствие: переименовал джобу — правь метку в ноутбуке и `job_name`
в правиле тем же коммитом, а старую строку `system_pulse` удаляй. И не удаляй
метку только потому, что она просрочена: просроченная метка живой джобы — это
сигнал о поломке, а не мусор. Удалять можно лишь метку имени, которого в
Databricks больше нет.

Одна джоба — одна метка. Два пульса из одного ноутбука (`Kabinet - Economics
Loader` и `... (LM)`, записанные с разницей в три секунды) — это не два сигнала,
а один, размноженный: правило висит на одном имени, второе не проверяется никем.
Если у джобы есть режимы, которые надо различать (утро и вечер), то это разные
метки И разные правила, а не одна метка на оба.

### Квота `createReport` — общая на весь аккаунт

SP-API даёт на создание отчёта один запрос в минуту с запасом в пятнадцать, и
это лимит **аккаунта продавца**, не джобы. Его делят все загрузчики отчётов
разом: Returns, MYI, Suppressed Listings, Catalog Items, SQP Loader с его
очередью, FBA Charges. Когда двое сходятся во времени, второй получает 429 —
и не на секунды, а на десятки минут: backfill FBA Charges 13.09.2026 ждал
сорок минут подряд и упал.

Практическое следствие: новые загрузчики отчётов ставим на часы, когда
остальные молчат, — смотреть расписания через Jobs API, а не помнить. На
создании отчёта ждать терпеливо, минутами, и писать в базу по частям — чтобы
падение на седьмом отчёте не уносило шесть скачанных. Backfill на много
отчётов — только в тихое окно и с поднятым таймаутом джобы.

Data Kiosk и Finances API этой квоты не касаются — у них свои.

### Квантификатор регулярки внутри f-строки — только в двойных скобках

SQL для Spark часто собирается f-строкой, и `'^[0-9]{6}'` внутри неё — это не
регулярка, а подстановка выражения `6`: шаблон молча превращается в `'^[0-9]6'`.
Ошибки нет, запрос выполняется, просто фильтр другой. Так `Kabinet - Stock
Loader` с 12.07 по 14.09.2026 брал из Odoo только SKU со второй цифрой 6 —
144 позиции на склад вместо ~3 000, и Мадрид в `warehouse_stock` показывал
133 штуки при 6 310 в источнике. Два месяца никто не заметил, потому что
строки были, просто мало.

Правило: в f-строке квантификаторы пишем `{{6}}`, `{{5,}}`; а лучше выносить
шаблон в переменную вне f-строки (как `NORM` там же рядом) и подставлять её.

## Слои данных

Три слоя, и у каждого свой хозяин по смыслу, а не по тому, кто первый успел.

| слой | где | что там |
|---|---|---|
| сырьё | `dnipro_m.raw_*` | ответ источника как есть: отчёт Amazon, выгрузка Mirakl, таблица Odoo. Одна таблица — один источник, без чистки и без бизнес-логики |
| расчёты | `kabinet_data` | то, что Кабинет считает поверх сырья: `economics_summary`, `coverage_summary`, справочники, инциденты, правила |
| витрины | вью поверх расчётов и сырья | что читают страницы и Tableau |

**Новый источник кладём в сырьё, не в свою базу.** Соблазн понятен: загрузчик
Кабинета, таблица в `kabinet_data`, всё под рукой. Но сырьё, спрятанное в слое
расчётов, для остальных не существует — Дарина не найдёт settlement-отчёты в
`kabinet_data`, заведёт свой загрузчик, и через месяц одни и те же отчёты Amazon
лежат в двух местах с разной глубиной и разными пропусками. Ровно так возникли
`raw_amazon_settlements_es` у неё и `raw_amazon_returns` у нас: обе про Amazon,
обе неполные, друг о друге не знают.

**Если источник уже собирается кем-то — не дублируем, а договариваемся о
переносе.** Прежде чем писать загрузчик, проверить `dnipro_m.raw_*` и спросить.
Нашёлся чужой, но неполный или заброшенный — расширяем его или забираем себе
целиком, с той же таблицей и теми же колонками, чтобы чужие вью не сломались.
Два загрузчика одного источника — это два хозяина строк, а к чему это приводит,
записано выше про LM в `economics_summary`.

Практическое следствие для Кабинета: `kabinet_data.raw_*` — это не сырьё, а
реплики и промежуточные выгрузки, которые Кабинету нужны локально в Lakebase.
Их первоисточник обязан лежать в `dnipro_m.raw_*`, и при расхождении прав тот,
что в `dnipro_m`.

## Факты о данных

Не выводится из кода и схемы — выяснялось по ходу задач, стоило времени.

- `sales_traffic_daily` — только Amazon: витринный отчёт Seller Central, каналов Mirakl (LM) в нём нет, и их отсутствие там нормально.
- Пустой фильтр в загрузчике означает «все рынки», поэтому выбор, схлопнувшийся в пустой после отсева каналов, надо обрабатывать отдельно, а не передавать дальше.
- Пустой COGS означает «не загружено», а не ноль: `COALESCE(cogs, 0)` даёт прибыль, равную выручке, и агрегировать такие поля надо через `sum(min_count=1)`.
- Ноль расходов на рекламу по каналам Mirakl — правда, а не пропуск данных: PPC там нет.
- `economics_summary` и `sales_traffic_daily` наполняются разными загрузчиками, поэтому период для карточек надо задавать общим якорем, а не `MAX(дата)` каждой таблицы по отдельности.
- Для Leroy Merlin `commission_fee` и `total_fees` — одно поле, а не два: комиссия площадки и есть все комиссии, и вычитать её из прибыли отдельно нельзя, «Чистыми» уже за её вычетом.
- У Amazon комиссия отдельным полем не приходит вообще: она внутри `total_fees` и не выделяется — так устроен SP-API, прочерк в колонке комиссии это норма, а не пропуск.
- Новая таблица, созданная пайплайном в Unity Catalog, в Lakebase сама не появляется: нужен настроенный synced table на Postgres-эндпоинт. Пока синка нет, объект существует в Databricks и отсутствует для Кабинета — запрос падает на «relation does not exist». Первый признак того, что дело в этом, а не в фильтрах: в Databricks строки есть, в `information_schema.tables` схемы `kabinet_data` объекта нет.
- Кабинет ходит в Lakebase как в обычный Postgres: база `databricks_postgres`, объекты — `kabinet_data.<имя>` без каталога. Каталог `dnipro_m` виден со стороны Databricks, но в запросах страницы его нет и быть не должно.
- AMC отдаёт данные только по инстансу Испании и скрывает строки, не прошедшие порог агрегации, — суммы всегда ниже, чем в рекламном кабинете. Скрытое приходит строкой с ключом `__MASKED__` (товары, запросы) или без `campaign_id` со статусом `masked` (кампании). В скрытой строке кампаний продаж обычно больше, чем во всех видимых вместе, поэтому прятать её нельзя: итог разойдётся с кабинетом втрое.
- У скрытых строк `reach` равен NULL намеренно: сумма уникальных пользователей по нескольким кампаниям не равна числу уникальных. Агрегировать через `sum(min_count=1)` — обычный `sum()` даёт ноль и превращает честную пустоту в неверное число.
- Атрибуция AMC закрывается 14 днями, основная масса покупок доезжает за трое суток. Свежая кампания попадает в `no_sales` не потому что плохая, а потому что покупки ещё не случились, — поэтому страница «Реклама» по умолчанию открывается на закрытом окне.
- `ads_alerts` считается загрузчиком по фиксированным окнам (`window_days` 7/30/90), по строке на окно. Произвольный диапазон среди них не считается, поэтому страница берёт ближайшее окно и подписывает, какое именно показано. Колонка `details` осталась в таблице, но Кабинет её НЕ читает: фраза собирается из чисел через i18n, иначе язык живёт вне словаря и в английском интерфейсе детали остаются русскими.
- Рынок записан в базе тремя способами: `economics_summary` и `listing_cards` — код страны (`DE`), `orders_history.sales_channel` — имя канала (`Amazon.de`), `asin_reviews_daily.marketplace` — хвост домена (`de`, `co.uk`). Третий ломается тише всех: `co.uk` не код страны, поэтому британские строки молча теряют заголовок и уезжают ссылкой на испанскую витрину. Приведение — только через `catalog._mk()`.
- `SUM()` в Postgres возвращает `numeric`, и до pandas он доезжает `Decimal`'ом: колонка становится object, а `st.column_config.NumberColumn` такую не форматирует — на экране пусто при живых числах рядом. Суммы, которые идут в числовую колонку, приводить в SQL через `::int`.
- Заголовок листинга по рынку есть только в `listing_cards` — это суточная реплика из другой Lakebase (проект listing-suite), кросс-проектные запросы Кабинет не делает. `economics_summary.product_name` для этой цели непригоден: под немецким рынком там лежит испанский текст, то есть рынок совпадает, а язык нет, и сравнением кодов рынка это не ловится.
- Главное фото товара — `listing_cards.main_image`, ключ `(asin, marketplace)`. Карточек меньше, чем товаров, поэтому «фото нет» и «товара нет» — разные вещи, и первое не повод прятать строку.
- `fba_inventory_deduped` дедуплицирован не до конца: он хранит снимки за разные дни и по строке на каждый `source_marketplace`. Без `snapshot_date = MAX(...)` и схлопывания рынков максимумом «Доступно» удваивается — 3 405 вместо 1 706.
- Перемещение между центрами — явный `event_type = 'WhseTransfers'` в `fba_ledger_detail`, пара всегда совпадает по `asin + msku + event_date`, направление по знаку `quantity`. Восстанавливать пары по совпадению даты и SKU нельзя: в пару попадают отгрузка клиенту и приёмка поставки.
- Даты прибытия поставки в данных FBA нет вообще: только три корзины inbound — working, shipped, receiving. Их надо складывать, одна колонка часто пустая.
- Склады FBA в `warehouses` — строки 84–90 «FBA XX», и имена эти зашиты в `Kabinet - Availability History` (`FBA_LOCATIONS`): загрузчик ищет склад по имени и заводит недостающий сам. Переименуешь — получишь дубль, как это уже было с 76–82 «Amazon FBA XX» (21.07–14.09.2026: маршруты смотрели на один набор, остатки писались в другой). Дубли удалены 14.09.2026, маршруты и рынки перенесены на 84–90.
- «RS Warszawa Spain» (Основной/Amazon/Карантин, id 34/36/35) с 14.09.2026 неактивны: физически это «RS Spain Madrid (Stock)» id 43, единственный склад отгрузки на маркетплейсы. Строки не удалены намеренно — старый ERP (`raw_erp_inventory_stock_auto`) ещё пишет под этими именами, и `daily_availability_history` продолжает ложиться в те же id. В Odoo (`raw_odoo_stock`) есть только Madrid и одиннадцать Tiendas.
- `warehouses.canonical_id` — не признак дубля, который можно скрыть: у «RS Warszawa Spain» (784 тыс. строк остатков, приоритет 1) он указывает на мадридский склад. Фильтровать справочник по `canonical_id IS NULL` нельзя, спрячется работающий склад.
- `coverage_norms` не читает никто, кроме собственной вкладки в «Справочниках». Статусы `ok/warning/critical` в `coverage_summary` считает загрузчик по своим порогам, а не по этой таблице, — значит заполнение справочника само по себе ни на одну цифру в Кабинете не влияет, пока расчёт не переведут на него.
- `kabinet_data.marketplaces` — представление над `marketplaces_new`, и внешние ключи чужих таблиц смотрят на `marketplaces_new`, а не на представление. Само по себе не ломается: id совпадают, — но при заведении новых ключей ссылаться надо на таблицу.
- Уникальность `coverage_norms` объявлена по тройке `(marketplace_id, pool_id, sku)`, а NULL в Postgres друг другу не равны: две строки с одним рынком и SKU при пустом `pool_id` ограничение НЕ нарушают. Дубли ловятся только проверкой в коде.
- `catalog_source` живёт не в базе Кабинета, а в Lakebase проекта listing-suite (`listing_data.catalog_source`), и Кабинет к ней не обращается вовсе. Искать её в `kabinet_data` бесполезно.
- Строки Leroy Merlin в `economics_summary` принадлежат одному загрузчику — `Kabinet - Leroy Merlin Transactions Loader`: он делает `DELETE … WHERE marketplace = 'LM'` и пишет их заново каждый прогон, причём без `commission_fee`. Ячейка 10 `Kabinet - Economics Loader` с 11.09.2026 только проставляет `commission_fee` у уже существующих строк и новых не создаёт. До этого там стоял UPSERT, и два загрузчика переписывали друг друга по кругу — чей прогон был последним, того цифры и лежали в витрине.
- Почему оффер LM не продаётся, лежит в `raw_lm_offers.inactivity_reasons` (с 11.09.2026 — колонка, до того только внутри `raw`). Значение `PRODUCT_STATUS_REJECTED` — отказ каталога Leroy Merlin по товару, а не проблема оффера: на 11.09 таких 282 из 292, активных девять. Именно это объясняет обвал продаж LM с 8 сентября, а не цены и не остатки.
- Settlement-отчёты Amazon (`GET_V2_SETTLEMENT_REPORT_DATA_FLAT_FILE_V2`) нельзя запросить через `createReport`: Amazon создаёт их сам по закрытии расчётного периода, забирать надо через `getReports` по типу. `createdSince` старше 90 дней API отвергает ошибкой `InvalidInput`, но внутри окна лежат отчёты с периодами двухлетней давности — у тихих рынков период не закрывается месяцами и закрывается разом. Поэтому история глубже 90 дней есть, но частями и с дырками, и качать её надо сразу: отчёт исчезает через 90 дней после создания, не после периода.
- Один settlement — один рынок, хотя `marketplaceIds` в метаданных отчёта перечисляет все 18 id аккаунта. Рынок берётся только из `marketplace-name` в строках; у отчётов из одних сервисных проводок (реклама, подписка) он пуст, и `marketplace` там NULL по-честному. Один и тот же settlement Amazon иногда выдаёт двумя `reportId` байт-в-байт — дедупликация по `settlement_id`, не по отчёту.
- Числа в settlement-файлах в локали рынка: EUR — `14356,05` с запятой, PLN — `200.44` с точкой; даты `DD.MM.YYYY [HH:MM:SS UTC]`. Парсер обязан держать оба формата, и проверка одна: сумма проводок равна `total-amount` итоговой строки — на 176 отчётах сходится до цента.
- В Workspace API `CAN_EDIT` на папку даёт править существующие ноутбуки, но не создавать новые — на импорт нового объекта нужен `CAN_MANAGE` на директорию (`Missing required permissions [Manage]`). Новые ноутбуки сервис-принципал положить не может, только файлом через человека.
- Составы наборов (ТЗ 005) уже собраны в сырье: `dnipro_m.raw_odoo_sku_mapping` — BOM из Odoo, 268 наборов `website_kit` из 160 компонентов плюс 2 741 моно-товар; `raw_amazon_sku_catalog` — та же связь для амазоновских SKU. В источнике Odoo есть дубли строк одного набора — при зеркалировании схлопываются с суммированием количества. Единый вид — `kabinet_data.v_sku_composition`, приоритет у Odoo. Покрытие: все 106 SKU с продажами за 90 дней находят состав точно; из 285 листингов не находят 11, и все 11 — мусорные суффиксы `-FBA-`, `-FBA_`, `amzn.gr.…`, снимаемые нормализацией до `sku_group`.
- Факт и намерение по ассортименту (ТЗ 007) живут в разных таблицах намеренно: `sku_lifecycle` загрузчик переписывает целиком, и ручная колонка в нём не пережила бы прогона. Намерение — `sku_target_status` с тем же ключом `(sku, marketplace)`, заполняется на «Справочники → Ассортимент». Прогноз должен читать намерение, а не факт: `not_launched` — это «не продавалось», а не «не будем».
- Метрику, посчитанную по части строк, подписываем долей выручки, а не числом SKU: «73 из 124» не говорит, три это процента оборота или девяносто, и процент маржи рядом читается неверно.
- Фактической даты отгрузки по Amazon в наших данных нет ни в одной витрине. В `get_orders` только обещания — `earliest_ship_date` и `latest_ship_date`, — плюс счётчики `number_of_items_shipped`. В Sales & Traffic есть `unitsShipped` и `shippedProductSales`, но они привязаны к дате заказа, а не отгрузки, и подменять одно другим нельзя. Ближайшее фактическое — `fba_ledger_detail` с `event_type = 'Shipments'`: реальное движение со склада, но разрезом «SKU × дата», без привязки к заказу.
- `orders_history.last_updated` — это отметка НАШЕГО загрузчика, а не `LastUpdateDate` от Amazon. Имя обманывает: у всех строк там одно и то же время последнего прогона, тогда как настоящая дата у Amazon разная по каждому заказу. Как заменитель даты отгрузки эта колонка не годится совсем — даст «сегодня» на любом заказе.
- Настоящий `LastUpdateDate` живёт только в Unity Catalog, в `raw_amazon_seller_central_get_orders.last_update_date`, и как заменитель даты отгрузки годится — проверено на шести месяцах. Медиана разрыва с датой заказа 1 день во все месяцы, p90 — 2-3 дня, в пределах трёх суток укладывается 90-96 % заказов. Важно: только при `order_status = 'Shipped'`. У `Unshipped` разрыв уезжает к 30 дням, потому что дата дрейфует у зависших заказов.
- Возврат `LastUpdateDate` не сдвигает — это проверено, а не предположено. На окне, где данные о возвратах пересекаются с заказами, у 85 заказов с возвратом разрыв оказался даже теснее, чем у 2 474 без него: медиана 1 день у обоих, максимум 7 дней против 92. Возврат Amazon пишет отдельным объектом, а не правкой заказа.
- `incidents.created_at` — `timestamp` БЕЗ зоны, а `resolved_at` и `now()` в коде — с зоной. psycopg2 отдаёт первое naive-датой, и любое `resolved_at - created_at` в Python падает `TypeError`. В `Kabinet - Watchdog` это падение сидело внутри `try` вокруг всего блока Telegram: джоба зелёная, пульс на месте, инциденты создаются — а в чат с 01.09 по 13.09.2026 не ушло ни одного сообщения. Читать колонку через `created_at AT TIME ZONE 'UTC'`; и не заворачивать отправку алерта в один `except` с расчётом текста — молчание канала должно быть красным, а не «⚠️» в логе, который никто не открывает.
- Атрибуция отзыв→запрос в `review_request_log.review_received_at` не работает и в текущем виде не заработает. `Listing Suite Sync Reviews Daily` считает её эвристикой «вырос `review_count` по ASIN → припиши последнему запросу за 30 дней», и джойн сравнивает `orders_history.marketplace_code` (`ES`) с `asin_reviews_daily.marketplace` (`es`) — поэтому ноль всегда. Но чинить регистр нельзя без второй правки: `review_count` у Amazon общий на семейство вариантов и скачет 8 → 537 при склейке карточек, эвристика припишет сотни «отзывов» запросам одного дня. Проверка `[ATTRIBUTION]` из Watchdog убрана 14.09.2026, инцидент закрыт вручную.
- `job_health_rules` проверяются по `job_id` через Jobs API, а не по пульсу: правило работает и для джобов без `system_pulse` (парсеры `parse_dnipro_m_*`). Пульс нужен для другого — для свежести данных и как внешняя точка, если умрёт сам сторож.
- Остатки своих складов приходят из двух систем, и у каждой свои склады. Старый ERP — `dnipro_m.raw_erp_inventory_stock_auto`, пишет джоб Дарины `Update ERP data` дважды в день (07:32 и 11:36 UTC, append ~17 тыс. строк): Piasecznie (основной и карантин), Тернополь и три «RS Warszawa Spain». Испанские строки там с 24.07.2026 сплошь нулевые — остатки Испании переехали в Odoo, но ERP продолжает выдавать склад с нулями. Odoo — `raw_odoo_stock` (Madrid + одиннадцать Tiendas, с 12.07) и `raw_odoo_madrid_stock` (только Madrid, с 06.05), обе живые. По Испании правда в Odoo.
- `warehouse_stock` в Кабинете нигде не показывается: его читает только ячейка переброски Stock Loader (доноры ERP) и расчёт карантина в автозаказе. «Остатки» на странице — это `stock_local` (FBA и Mirakl), а не свои склады.
- `warehouses.stock_role` (с 14.09.2026): `quarantine` — склад «ожидает проверки». Донором переброски не бывает и приоритет 0, но его остаток вычитается из «Заказать» в `reorder_recommendations` вместе с товаром в пути (`in_transit_qty`, `quarantine_qty`) — один механизм на оба случая, решение владельца. Срочность (urgency) pipeline не меняет: она про риск обнуления витрины, а товар на проверке продавать ещё нельзя.
- Имена складов FBA в `supply_chains`-маршрутах и в `FBA_DEST_MAP` Stock Loader должны совпадать со строками `warehouses` 84–90 «FBA XX». После чистки дублей 14.09.2026 старая карта «Amazon FBA XX» дала «0 активных fba_inbound», и переброска на один прогон осталась без Мадрида и ERP.
- Brand Analytics (SP-API, `GET_BRAND_ANALYTICS_*`): из пяти отчётов работают два. Item Comparison и Alternate Purchase Amazon отвечает `This report is now deprecated` (14.09.2026, WEEK и MONTH). Market Basket работает без дополнительной роли. Top Search Terms по PL и BE — FATAL «client error», аккаунту недоступен. Отчёт за неделю по одному рынку — 143–465 тыс. фраз (ES 162 МБ, DE больше), `dbutils.fs.put` такое не берёт (лимит 128 МБ) — сырьё пишем сжатым через файловый путь Volume. Данных по неделе с воскресенья по субботу.
- Top Search Terms хранится с отбором по `kabinet_data.ba_search_term_rules` (top_n / category / exclude на рынок), не целиком. «Первые 5 000 фраз» почти не про нас: в ES из топ-5000 нашей категории 36 фраз, наши ASIN в топ-3 кликов — 9 фраз за неделю. Категорийная регулярка даёт 800–3 000 фраз на рынок; словарь и стоп-слова править в таблице, не в коде.
- Строка `reorder_recommendations` с 15.09.2026 — базовый артикул, не SKU канала: «41324000» и «41324000-FBA» складываются в одну, варианты-комплекты (-A, -B, S_…) остаются отдельными товарами. Товар «в пути» — это `max(Inbound Amazon, ТТН ERP «В пути») + транзит Польша→Мадрид`; накладные ERP «В пути» из Piasecznie — почти сплошь розничные ТТН по 1–2 шт с кодом PLEU, пополнение FBA видно только в корзинах `afn_inbound_*` из `fba_inventory` (15.09: 14 SKU / 752 шт против 3 пересечений с ТТН). Сегодняшний расчёт перед записью удаляется целиком, кроме строк со статусом `ordered` — UPSERT по ключу оставлял строки прошлого прогона, если ключ поменялся.
- Сроки цепочки поставки (Дарина, 15.09.2026) лежат в `supply_chains`, не в коде: Украина → Piasecznie 14 дней (маршрут 37→32), Piasecznie → Мадрид 8 дней (маршрут id 2; было экспертное «2 дня») плюс до 5 дней ожидания рейса — машина в Мадрид ходит по средам (`reorder_params.madrid_dispatch_wait_days`). Итого 22–27 дней. В `raw_erp_transportation_invoices` этих плеч нет — только последняя миля. Срок поставки в автозаказе считается по плечу ближайшего пополнения: PL (карантин или остаток Piasecznie) = 8 + ожидание + 3 до FBA ES = 16; UA (план Poland-Spain или остаток Тернополя) = 14 + PL = 30; поставщик — маршрута нет, `reorder_params.supplier_lead_days` = 30 как ОЦЕНКА, и в `reorder_recommendations.lead_source` это написано словом. Констант срока в коде больше нет; страховой запас — `reorder_params.safety_days`.
- Карантин по сути один — Piasecznie. «Тернополь Подольская, 21 Європа (Карантин)» по имени карантин, по факту основной склад Украины: срок выхода не закладывается, в вычет автозаказа не входит совсем. Вместо него вычитается план снабжения — `supply_poland_spain.quantity_manager` (лист Poland-Spain Дарины, что реально запланировано к отправке в Испанию), колонка `planned_qty`. Отсев на карантине — `reorder_params.quarantine_reject_pct`, пока ноль, Дарина уточнит у снабжения.
- Возраст запасов FBA: `GET_FBA_INVENTORY_AGED_DATA` Amazon больше не отдаёт (FATAL, 15.09.2026). Корзины 0–90 / 91–180 / 181–270 / 271–365 / 366–455 / 456+, единицы и суммы надбавки за возраст (AIS) по корзинам, плата за хранение на следующий месяц и рекомендация Amazon лежат в `GET_FBA_INVENTORY_PLANNING_DATA` — его и тянет `Kabinet - Inventory Age Loader` в `dnipro_m.raw_amazon_fba_inventory_planning` (реплика в `kabinet_data`). Отчёт отдаётся по рынку, но запас pan-EU: один и тот же FNSKU повторяется в ES/DE/FR/IT с почти теми же корзинами — суммировать рынки нельзя, брать один или схлопывать по fnsku.
- Account Health в SP-API есть только отчётом `GET_V2_SELLER_PERFORMANCE_REPORT` (JSON по рынку: статус аккаунта, `accountHealthRating` со score, ODR AFN/MFN, LSR, отмены, OTDR, VTR, дефекты счетов, нарушения политик по видам, запросы документов). Прямого метода нет; V1 — XML со статусами Good/Bad без чисел. Загрузчик `Kabinet - Account Health Loader` → `dnipro_m.raw_amazon_seller_performance` (+ реплика).
- ТЗ 004 v0.2 (06.09.2026): пул — только marketplace **одной страны**, минимум два участника, отдельного флага «активен» нет, исключения для Pan-EU нет. Черновики от 14.09 (собранные по складам: «Amazon EU» на 9 стран, «ManoMano» ES+FR, три одиночных) удалены 17.09.2026 по решению владельца; действующие пулы — Spain (AMZ-ES, LM-ES, MM-ES, CF-ES, id 6) и France (AMZ-FR, MM-FR, id 7). DE, IT, NL, BE, GB, SE, IE, PL — без пула, прогноз по marketplace.
- Реестр прогноза (ТЗ 010/011) — `forecast_documents`, `forecast_register`, `forecast_change_log`, вью `v_forecast_current` (с 17.09.2026). Месяц — DATE первого числа; объект — `(object_type, object_id)` на `marketplaces_new` или `pools`; состав пула фиксируется в документе jsonb; действующая запись — `is_current`, одна на объект × SKU × месяц (частичный уникальный индекс). Зависимая потребность — те же строки с `record_type = 'dependent'`. Факт для отчёта «Факт / прогноз» по ТЗ — по дате **отгрузки**, не заказа: в наших данных для Amazon это `LastUpdateDate` при `Shipped` (см. факт выше), для LM — `shipped_date`, для MM/CF даты отгрузки нет.
- Снимки витрины (`listing_data.listing_snapshots`, Lakebase listing-suite) с 17.09.2026 реплицируются в `dnipro_m.raw_amazon_listing_snapshots` джобом `Listing Suite - Snapshots Replica` (15:30 Kyiv, инкремент по `fetched_at`, MERGE по `id`). BSR в снимке не отдельное поле: он лежит в `raw.product_information` под ключом на языке витрины («Clasificación en los más vendidos de Amazon», «Amazon Bestseller-Rang») строкой «nº64.863 en Jardín … nº451 en Recortadoras» — реплика парсит первую пару в `bsr_rank`/`bsr_category`, полный текст в `bsr_text`. Цена со скидкой — `display_price`, цена до скидки — `previous_price_text` (строка «254,98€»), купон — `has_coupon`.
- Дата отгрузки по Mirakl есть только у Leroy Merlin — `raw_lm_order_lines.shipped_date`, заполнена на 100 %. У ManoMano и Carrefour такого поля нет ни в заказах, ни в строках: есть статус `SHIPPED`, но не дата его наступления, а истории смены статусов мы не храним.

## Пороги и правила живут в БД

Пороги свежести, здоровья загрузчиков и прочие правила лежат в `system_pulse`,
`data_freshness_rules`, `job_health_rules`. Код читает их оттуда и НЕ дублирует
константами.

Иначе правило меняют в базе, а поведение остаётся старым — и расхождение не видно
ниоткуда: в базе новое значение, на экране результат по старому, и оба выглядят
правдоподобно. Ищется это только чтением кода.

Три случая за два дня: фильтр маркетплейса, порог SQP, плашка свежести с зашитым
`hours_ago > 26`.

Практическое следствие: увидев в коде число, которое похоже на настройку — часы,
дни, доли, границы «нормально/плохо», — сначала проверь, нет ли для него строки в
БД. Если есть, читай оттуда. Если нет, но по смыслу должна быть, скажи об этом,
а не закрепляй константу.

## Метрики и фильтры качества

- Подстановки в переводы делаются через `t(key, a=..., b=...)`, а не `t(key).format(...)`. Обычный `.format()` на переводе бросает KeyError, когда набор плейсхолдеров в словаре и в коде разошёлся, — и это роняет страницу целиком вместо того, чтобы показать прочерк в одном месте. Расходятся они легко: фраза и код живут в разных файлах, а Streamlit Cloud после деплоя без полного перезапуска умеет держать старый модуль рядом с новым.
- `x or ""` для приведения к тексту не годится: NaN в Python истинен, поэтому выражение возвращает сам NaN и следующий `.lower()` падает; `pd.NA` бросает TypeError ещё на проверке истинности. Обёртка `str()` спасает от падения, но подставляет строку «nan», и та уезжает в таблицу как артикул. Единственная верная проверка — `pd.isna`, и живёт она в `util.as_text`.
- Фильтры качества данных не применяются к живому хвосту без явного индикатора отсева — три инцидента класса: обрыв графика отзывов, усыхание корзины тотала, молчаливый обрыв 16.08.
- Критерии аномалий строить на суточных скачках, не на размахе за окно.
